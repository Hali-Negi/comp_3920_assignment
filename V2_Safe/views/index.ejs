<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <% if (heading) { %>
            <h1><%= heading %></h1>
        <% } else { %>
            <h1>Welcome</h1>
        <% } %>
        
        <div class="button-group">
            <a href="<%= urls[0] %>" class="btn"><%= btn1 %></a>
            <a href="<%= urls[1] %>" class="btn btn-secondary"><%= btn2 %></a>
        </div>
    </div>
</body>
</html>